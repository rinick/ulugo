{
  "name": "@ulugo/scoring-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ulugo/go-core": "workspace:*",
    "@ulugo/sgf-core": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.0.14"
  }
}
